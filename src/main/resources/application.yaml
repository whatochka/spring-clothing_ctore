spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/eshop
    username: eshopAdmin
    password: 358000
  flyway:
    baseline-on-migrate: true
  mvc:
    favicon:
      enabled: false
server:
  port: 35664
